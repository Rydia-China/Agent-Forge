generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// placeholder — 按需替换为业务模型
model Config {
  key       String   @id
  value     String
  updatedAt DateTime @updatedAt
}
