# 短期目标

> 唯一规划文件。所有条目完成前不得开发新功能。
> 完成后删除该条目并提交。新需求追加到末尾。

## Phase 7 — Skill & MCP 版本管理
- Update 不覆盖，追加新版本（旧版本不可变）
- 每个版本可独立查看
- production 指针：指定某版本为生效版本，agent 运行时只使用 production 版本
- MCP 支持回滚：切换 production 指针后自动 reload sandbox

## Phase 8 — UI
- 基础 Chat UI
